{
  "name": "coincard",
  "private": true,
  "workspaces": [
    "client",
    "shared"
  ],
  "scripts": {
    "dev": "npm run build:shared && concurrently \"npm run dev:java\" \"npm run dev --workspace client\"",
    "build:shared": "npm run build --workspace shared",
    "build:client": "npm run build:shared && npm run build --workspace client",
    "build:java": "node scripts/maven.mjs package -DskipTests",
    "build": "npm run build:client && npm run build:java",
    "verify": "npm run build:shared && npm run typecheck --workspace client && npm run test:java && npm run build && node scripts/verify-e2e.mjs",
    "start": "java -jar server-java/target/coincard-server-0.0.1.jar",
    "tunnel": "node scripts/tunnel.mjs",
    "test": "npm run test:java",
    "test:java": "node scripts/maven.mjs test",
    "dev:java": "node scripts/maven.mjs spring-boot:run",
    "dev:client": "npm run build:shared && npm run dev --workspace client",
    "test:e2e": "node e2e-two-players.cjs",
    "test:e2e-neg": "node e2e-negative.cjs",
    "test:e2e-connection": "node e2e-connection.cjs",
    "test:e2e-tabs": "node e2e-tabs.cjs"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "ws": "^8.18.3"
  }
}
